name = "calmmini-pay"
main = "worker.js"
compatibility_date = "2024-11-01"

[vars]
SITE_URL = "https://www.calmmini.nl"
SHOP_EMAIL = "info@calmmini.nl"
SHIPPING = "8.00"

# GEHEIME sleutels als "Secret" toevoegen in het Cloudflare-dashboard
# (of: npx wrangler secret put MOLLIE_API_KEY):
#   MOLLIE_API_KEY   -> je Mollie API key (test_... of live_...)
#   WEB3FORMS_KEY    -> je Web3Forms access key (dezelfde als op de site)

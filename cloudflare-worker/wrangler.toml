name = "calmmini-pay"
main = "worker.js"
compatibility_date = "2024-11-01"

# Niet-geheime instellingen (mogen hier staan):
[vars]
SITE_URL = "https://www.calmmini.nl"
SHOP_EMAIL = "info@calmmini.nl"
FROM_EMAIL = "CALMMINI <bestelling@calmmini.nl>"
SHIPPING = "4.95"

# GEHEIME sleutels NIET hier zetten. Voeg ze toe als "Secret" in het
# Cloudflare-dashboard (of met: npx wrangler secret put MOLLIE_API_KEY):
#   MOLLIE_API_KEY
#   RESEND_API_KEY
